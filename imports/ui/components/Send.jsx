import styled from 'styled-components';

const Send = styled.form`
    display:flex;
    flex-direction: column;
    position: relative;
    flex-wrap: wrap;
    width: 400px;
    left: calc(50% - 180px);

`;


export default Send;
